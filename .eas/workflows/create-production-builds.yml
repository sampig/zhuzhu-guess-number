name: Expo Create Production Builds

on:
  push:
    branches: ['main']

jobs:
  build_android:
    name: Build Android
    type: build # This job type creates a production build for Android
    params:
      platform: android
  # build_ios:
  #   type: build # This job type creates a production build for iOS
  #   params:
  #     platform: ios
